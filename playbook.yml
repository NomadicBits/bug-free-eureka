---
- name: Setup Bazzite Extra Packages
  hosts: localhost
  connection: local
  vars_files:
    - vars/packages.yml

  tasks:
    - name: Run setup tasks
      ansible.builtin.include_tasks: tasks/main.yml
